@model LoL.Models.PlayerMasterLeague
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ChampionMastersInit</title>
</head>
<body>
    <div> 
        @foreach (var m in Model.entries)
        {
            <h4>@m.playerOrTeamId</h4>
        }
        <h4>@ViewBag.Count</h4>
    </div>
</body>
</html>
